{{# hasHeadings page=this }}
  {{# eachHeading page=../this }}
    <li class="{{# if draft }}draft{{/ if }}">
      <a href="#{{ id }}" class="smooth-scroll">
        {{ name }}
      </a>
    </li>
  {{/ eachHeading }}
{{/ hasHeadings }}
