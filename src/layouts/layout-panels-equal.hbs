---
layout: layout-showcase
patternShowcase:
  sameHeight: true
  type: panel
---

{{# if markdown }}
  {{# markdown }}{{ body }}{{/ markdown }}
{{ else }}
  {{# withNewline }}{{ body }}{{/ withNewline }}
{{/ if }}