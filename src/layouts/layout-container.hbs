---
layout: layout-base
---

<div class="content">
  <div class="{{ stache.config.bootstrap_container }}">
    <div class="row">
      <div class="col-sm-12">
        {{# inherit stache.config.markdown markdown }}
          {{# markdown }}{{ body }}{{/ markdown }}
        {{ else }}
          {{# withNewline }}{{ body }}{{/ withNewline }}
        {{/ inherit }}
        {{# inherit stache.config.comments comments }}
          {{ include 'partial-disqus' }}
        {{/ inherit }}
      </div>
    </div>
  </div>
</div>
